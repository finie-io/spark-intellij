kind: Test
metadata:
  name: ${NAME}
  description: ""
spec:
  endpoint: webserver-get-root
  services:
    - ref: webserver
  execution:
    executor: http
    target: http://webserver
    request:
      method: GET
      url: /
  assertions:
    - response:
        statusCode: 200
